# Django (auth, models, APIs)
DJANGO_SECRET_KEY=your-secret-key-change-in-production

# FastAPI uses same key for JWT validation
# Optional: point to Django DB for /ai/simulations
# DJANGO_DB_PATH=/path/to/django_core/db.sqlite3

# Telegram (alerts)
# TELEGRAM_BOT_TOKEN=your-bot-token

# Redis (Celery)
# CELERY_BROKER_URL=redis://localhost:6379/0
